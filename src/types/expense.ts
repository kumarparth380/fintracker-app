export type ExpenseStats = {
  entertainment: number;
  investment: number;
  billExpenses: number;
  others: number;
};

export type ExpenseData = {
  name: string;
  value: number;
  color: string;
};
